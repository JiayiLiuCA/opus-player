import React from 'react';
import styles from './PlayingBar.css';

const PlayingBar = () => {
  return (
    <div className={styles.playingBar}>
      <p>PlayingBar</p>
    </div>
  );
};

export default PlayingBar;
